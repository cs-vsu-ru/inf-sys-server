name: Redeploy Image on Host

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - name: Execute a sample command
        run: touch docker-compose.yaml